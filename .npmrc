# Hoist all transitive deps into a single node_modules tree.
# Expo / React Native / Metro work best with a flat layout; pnpm's
# default strict isolation breaks Metro's package resolution.
node-linker=hoisted

# Install peer deps automatically so Expo's many peer requirements
# don't require manual intervention.
auto-install-peers=true
